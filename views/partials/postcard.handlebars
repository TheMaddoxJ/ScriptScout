<div class="card">
    <div class="card-header">
        <h2>{{title}}</h2>
    </div>
        <h3>Location: {{location}} Salary: ${{salary}}</h3>
    <div class="card-body"> 
        <p>{{content}}</p>
    </div>
</div>         
 
