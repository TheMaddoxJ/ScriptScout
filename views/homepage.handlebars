<div class="posts d-flex justify-content-center">
    <div class="d-block col-6">
        {{#each posts as |post|}}
        <div class="mt-2">
            {{> postcard post}}
        </div>
        {{/each}}
    </div>
</div>